query cumCardLayout($docId: Long) {
  cumlistCumListByDocId(docId: $docId) {
    docId
    document {
      state {
        name
      }
    }
    operationsNts {
      isWaiting
    }
  }
}
